
# ---- CTF file shares (appended by provision.sh) ----

[records]
    comment = Candyland estate records
    path = /srv/shares/records
    read only = yes
    guest ok = no

[sql_backups]
    comment = SQL nightly backups
    path = /srv/shares/sql_backups
    read only = yes
    guest ok = no
    valid users = CANDYLAND\svc_sql

[vault]
    comment = The vault
    path = /srv/shares/vault
    read only = yes
    guest ok = no
    # Domain Admins only. calvin.candie is the DA you pass-the-hash as.
    valid users = CANDYLAND\calvin.candie CANDYLAND\Administrator
